#include "rn.h"

double	ft_id(double nb)
{
	printf("et oui ! pute\n");
	return (nb);
}

double	ft_squish(double nb)
{
	return ((nb + 5) / 10);
}

double	ft_choose(double nb)
{
	if (nb >= 0.5)
		return (1);
	else
		return (0);
}
